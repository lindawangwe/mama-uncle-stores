# MERN E-commerce App (Work in Progress)

A full-stack MERN app (Vite + TypeScript frontend, Express + Node backend) with authentication, product listings and Stripe payments. This repo contains both `frontend/` and `backend/`. 🚧

## Tech stack
- Frontend: React (Vite + TypeScript), Zustand, Axios
- Backend: Node.js, Express, MongoDB (Mongoose)
- Payments: Stripe

## Features (WIP)
- User signup & login (JWT)
- Product listing & search
- Shopping cart & checkout (Stripe)

## Quickstart (local)
1. Clone:
   ```bash
   git clone https://github.com/your-username/your-repo.git
   cd your-repo
